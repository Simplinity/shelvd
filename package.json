{
  "name": "shelvd",
  "version": "0.16.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/www",
    "build": "npm run build --workspace=apps/www",
    "start": "npm run start --workspace=apps/www",
    "lint": "npm run lint --workspace=apps/www"
  }
}
